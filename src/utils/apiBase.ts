// Absolute base URL for the API backend (Cloudflare Pages Function).
// Used so the app works identically whether served from dorpts.web.app
// (Firebase Hosting) or dorpts.pages.dev (Cloudflare Pages).
export const API_BASE = "https://dorpts.pages.dev";
